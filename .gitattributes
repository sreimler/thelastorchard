# Default: normalize all text files to LF
* text=auto eol=lf

# Windows batch files need CRLF
*.bat text eol=crlf

# Tiled map files - explicitly mark as text with LF
*.tmx text eol=lf
*.tsx text eol=lf

# Binary files - never touch
*.png binary
*.jpg binary
*.jpeg binary
*.jar binary
